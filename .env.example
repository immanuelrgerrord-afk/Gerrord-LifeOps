# LifeOps application configuration
# Copy to .env.local for local overrides (not committed).

VITE_APP_NAME=LifeOps
VITE_APP_VERSION=1.0.0
VITE_DEFAULT_THEME=auto
VITE_DEFAULT_CURRENCY=INR
VITE_DEFAULT_LOCALE=en-IN

# Reserved for future cloud sync (leave empty for offline-only mode)
VITE_API_URL=
